/** Peacock teal aligned with the tab favicon — used for `theme-color` where browsers apply it. */
export const SITE_THEME_COLOR = "#347474";

/** Site logo served from `public/brand/logo.png`. */
export const SITE_LOGO_SRC = "/brand/logo.png";

/** Tab favicon when `data-theme="dark"` — 48px square; same as default HTML icon for search engines. */
export const SITE_FAVICON_DARK_SRC = "/favicon.png";

/** Tab favicon when `data-theme="light"` — baked filter look; regenerate via `npm run icons:favicon-light`. */
export const SITE_FAVICON_LIGHT_SRC = "/brand/favicon-light.png";
